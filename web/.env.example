# Copy to web/.env.local. The dashboard logs in as the read-only AIM_WEB user,
# which can SELECT from the AIM_V_* views and nothing else.
AIM_WEB_USER=aim_web
AIM_WEB_PASSWORD=
AIM_DB_DSN=localhost:1521/FREEPDB1
AIM_SCHEMA=AIM_APP
